/* modules/scoring — the Scores product (composite, configurable, deterministic). */
export { scoreArea, parseScoreBody, applyWeights, dimensionKey, PRESET_DIMENSION_KEYS, type ScoreQuery } from "./score";
