# env/prod/postgres.env.example -- container/postgres image, prod (Plan 008).
#
# Neon is the production database. This file exists for parity / disaster
# recovery only -- the container/postgres image is NOT a production target.
# Filling this in implies a deliberate self-hosted DR plan; coordinate with
# plan 009 before doing so.

POSTGRES_DB=oga
POSTGRES_USER=oga
POSTGRES_PASSWORD=
# PGDATA=/var/lib/postgresql/data
