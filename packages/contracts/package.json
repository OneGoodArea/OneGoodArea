{
  "name": "@onegoodarea/contracts",
  "version": "0.0.0",
  "private": true,
  "description": "Shared TypeScript types (and, from Phase 2, Zod schemas) for OneGoodArea apps/web and apps/api. No DB or runtime code — contracts only.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  }
}
