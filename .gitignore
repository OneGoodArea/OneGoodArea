# dependencies
/node_modules
node_modules/
LINKEDIN.md
OVERVIEW.md
METHODOLOGY-INTERNAL.md
AUDIT-CODE.md
AUDIT-SEO.md
PRODUCT.md
# internal strategy + audit + dev log — keep private
DEVELOPMENT-LOG.md
strategic_brief.md
AUDIT-ONEGOODAREA.md
DESIGN-SYSTEM-SPEC.md
PRICING-V2-PROPOSAL.md
MASTER-PROPOSAL.md
EXECUTION-PLAYBOOK.md
PRODUCT-POSITIONING.md
STRATEGY.md
ARCHITECTURE.md
/.pnp
.pnp.js
.yarn/install-state.gz

# testing — generated artifacts (vitest reports + coverage). Plan 007
# phase 0 routes all of these under /.artifacts/ at repo root; per-
# workspace tools (apps/api, apps/web, packages/contracts) write into
# .artifacts/{test-reports,coverage}/<workspace>/. The legacy
# /test_reports (typo, never used) and /coverage paths kept as belt-
# and-braces in case a tool writes to the old locations.
/.artifacts/
/coverage
/test_reports
/test-reports
# next.js (app now lives at apps/web; keep these unanchored so the workspace's
# build output is ignored too)
.next/
/out/
apps/web/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env files
.env*.local
.env
.env.local.test
.env.local.test.secrets
*.secrets
vercel-import.env

# vercel
.vercel

# typescript
*.tsbuildinfo
tsconfig.tsbuildinfo
next-env.d.ts

# internal Claude-authored strategy + architecture snapshots
STRATEGY.md
ARCHITECTURE.md

# design-v2 build artefacts — large topojson fetched once, SVG paths inlined in client.tsx
scripts/world-*.json
scripts/uk-paths.json

# claude code local state
.claude/
.aider*

# apps/api production bundle (built in-image / by `npm run build`)
apps/api/dist/

# Personal narrative / story doc (keep private)
STORY.md
